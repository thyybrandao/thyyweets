import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:thyyweets/app/modules/splash/pages/intro/intro_page.dart';

void main() {
  testWidgets('IntroPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(IntroPage(title: 'Intro')));
    //  final titleFinder = find.text('Intro');
    //  expect(titleFinder, findsOneWidget);
  });
}
